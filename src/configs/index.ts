import APP_CONFIG from './app.config';
import MIDDLEWARE_CONFIG from './middleware.config';
import SWAGGER_CONFIG from './swagger.config';

export default [APP_CONFIG, MIDDLEWARE_CONFIG, SWAGGER_CONFIG];
